# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $alt Mod1

# font for window titles. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal
#bindsym $mod+Return exec terminator --title=terminal

# kill focused window
bindsym $mod+Shift+Q kill
bindsym $alt+F4 kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+H move left 30 px
bindsym $mod+Shift+J move down 30 px
bindsym $mod+Shift+K move up 30 px
bindsym $mod+Shift+L move right 30 px

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+$alt+h split h

# split in vertical orientation
bindsym $mod+$alt+v split v

# enter fullscreen mode for the focused container
bindsym $mod+$alt+f fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+$alt+s layout stacking
bindsym $mod+$alt+w layout tabbed
bindsym $mod+$alt+e layout default

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindcode $mod+d focus child

# customize workspace names
set $ws1 1: terminals
set $ws2 2: coding
set $ws3 3: design
set $ws4 4: gimp
set $ws5 5: surf
set $ws6 6: comms
set $ws7 7: music
set $ws8 8: video
set $ws9 9: transcoding
set $ws10 10

# switch to workspace
#bindsym $mod+1 workspace 1: terminals
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# move focused container to workspace
#bindsym $mod+Shift+exclam move container to workspace 1: terminals
bindsym $mod+Shift+exclam move container to workspace $ws1
bindsym $mod+Shift+at move container to workspace $ws2
bindsym $mod+Shift+numbersign move container to workspace $ws3
bindsym $mod+Shift+dollar move container to workspace $ws4
bindsym $mod+Shift+percent move container to workspace $ws5
bindsym $mod+Shift+asciicircum move container to workspace $ws6
bindsym $mod+Shift+ampersand move container to workspace $ws7
bindsym $mod+Shift+asterisk move container to workspace $ws8
bindsym $mod+Shift+parenleft move container to workspace $ws9
bindsym $mod+Shift+parenright move container to workspace $ws10

# reload the configuration file
bindsym $mod+Shift+C reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+R restart
# exit i3 (logs you out of your X session)
#bindsym $mod+Shift+E exit

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym 113 resize shrink width 10 px or 10 ppt
        bindsym 116 resize grow height 10 px or 10 ppt
        bindsym 111 resize shrink height 10 px or 10 ppt
        bindsym 114 resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
    colors {
        # grays
		background #2E2E3F
		statusline #7777AA
		focused_workspace  #7777AA #2E2E3F #7777AA
		active_workspace   #4B4B6B #2E2E3F #4B4B6B
		inactive_workspace #4B4B6B #2E2E3F #4B4B6B

        # greens
		# background #002200
		# statusline #4AA866
		# focused_workspace  #4AA866 #002200 #4AA866
		# active_workspace   #00FFFF #002200 #ffffff
		# inactive_workspace #0D6611 #002200 #0D6611

        # cyans
		# background #FFFFFF
		# statusline #004242
		# focused_workspace  #007070 #004242 #00FFFF
		# active_workspace   #00FFFF #5f676a #ffffff
		# inactive_workspace #FFFFFF #FFFFFF #004242

        # golds
		# background #160902
		# statusline #7E6E35
		# focused_workspace  #160902 #7E6E35 #160902
		# active_workspace   #00FFFF #5f676a #ffffff
		# inactive_workspace #7E6E35 #160902 #7E6E35
		# urgent_workspace   #2f343a #900000 #ffffff
    }
    status_command ~/.i3/clisi3.sh
    position bottom
    font pango:DejaVu Sans Mono 10
}

# automatic window assignments
assign [class="Google-chrome"] $ws5
assign [class="Gimp"] $ws4
assign [class="Geany"] $ws2
assign [class="Dvdrip"] $ws9

# do not follow the white rabbit
focus_follows_mouse no

# float notifications
# + make floating
# + steal focus back
# + remove the border
for_window [class="Xfce4-notifyd"] floating enable focus tiling border none

# float these windows too
for_window [class="milkytracker"] floating enable
for_window [class="XCalc"] floating enable
for_window [class="MPlayer"] floating enable
for_window [class="Eclipse"] floating enable
for_window [class="ADT"] floating enable
for_window [class="mplayer2"] floating enable
for_window [class="Qemu-launcher"] floating enable
for_window [class="kvm"] floating enable
for_window [class="qemu-system-i386"] floating enable
for_window [class="Gcolor2"] floating enable
for_window [class="Viewnior"] floating enable
for_window [class="Unetbootin"] floating enable
for_window [class="Nitrogen"] floating enable
for_window [class="dosbox"] floating enable
for_window [class="Geany" title="Commit"] floating disable
for_window [title="Alive"] floating enable
for_window [title="Crunchbang Project"] floating enable

# mod tab back and forth between workspaces
bindsym $mod+Tab workspace back_and_forth

# show and hide scratchpad windows
bindsym $mod+Shift+minus move scratchpad
bindsym $mod+minus scratchpad show

# prev next workspaces
bindsym $mod+$alt+j workspace prev
bindsym $mod+$alt+k workspace next

# grays
# class             	border      background      text        indicator
client.focused      	#7777AA     #7777AA         #ffffff     #66AA63
client.focused_inactive #2E2E3F     #2E2E3F         #4B4B6B     #003333
client.unfocused    	#2E2E3F     #2E2E3F         #4B4B6B     #003333

# gold colors
# class             	border      background      text        indicator
# client.focused      	#160902     #7E6E35         #FFFFFF     #dddd00
# client.focused_inactive #7E6E35     #160902         #7E6E35     #333300
# client.unfocused    	#7E6E35     #160902         #7E6E35     #333300

# green bright
# class             	border      background      text        indicator
# client.focused      	#00dd00     #00bb00         #000000     #00dddd
# client.focused_inactive #003300     #002200         #888888     #003333
# client.unfocused    	#003300     #002200         #888888     #003333

# green dark
# class             	border      background      text        indicator
# client.focused      	#66AA63     #002200         #66AA63     #66AA63
# client.focused_inactive #003300     #002200         #0D6611     #003333
# client.unfocused    	#003300     #002200         #0D6611     #003333

# cyan colors
# class             	border      background      text        indicator
#client.focused      	#004242     #00FFFF         #004242     #dddd00
#client.focused_inactive #004242     #004242         #00FFFF     #333300
#client.unfocused    	#004242     #004242         #00FFFF     #333300

# auto start apps
exec /home/kbmonkey/bin/auto-start

