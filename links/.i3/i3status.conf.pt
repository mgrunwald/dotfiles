general {
        colors = true
        interval = 5
}

order = "disk /"
order += "disk /home"
order += "load"
order += "time"

time {
	format = "%Y-%m-%d %H:%M:%S"
}

load {
	format = "%1min"
}

disk "/" {
	format = "%free"
}

disk "/home" {
	format = "%free"
}
